esphome:
  project:
    name: "chriopter.hall-9"
    version: "1.1"
    
text_sensor:
  - platform: template
    name: "Hall 9 Project Version"
    id: project_version
    update_interval: never
    lambda: |-
      return {"${project_version}"};
